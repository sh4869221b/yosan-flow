# Issue #224 E2E flake investigation record

## Scope and evidence policy

- Issue: [#224](https://github.com/sh4869221b/yosan-flow/issues/224)
- Goal: identify the root cause of intermittent `E2E tests` failures without hiding it with unconditional retries.
- Facts below are limited to live GitHub Actions metadata/logs and the current source. Candidate causes remain hypotheses until their declared RED/GREEN gate succeeds.
- Raw GitHub logs may contain runner identifiers and credential setup details. The checked-in record only quotes redacted, decision-relevant lines; `.omo/evidence/issue-224/task-1-baseline.md` contains the command ledger.

## Observed baseline

All timestamps are GitHub UTC timestamps with a derived JST rendering (`UTC+09:00`). The JST rendering is an arithmetic conversion, not proof of a clock-boundary race.

| Run                                                                              | Event / SHA                 | Run window (UTC; derived JST)                 | E2E runner region | Observed failure                                                                                                                                                                                      | Artifacts        |
| -------------------------------------------------------------------------------- | --------------------------- | --------------------------------------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| [29133029655](https://github.com/sh4869221b/yosan-flow/actions/runs/29133029655) | `push` / `7f164914`         | `00:38:13–00:41:46Z`; `09:38:13–09:41:46 JST` | `westus3`         | `dashboard-day-entry-add-edit.spec.ts:36` could not find the saved memo within 5 s; `dashboard-day-entry-delete-recalculation.spec.ts:22` timed out at 120 s clicking `保存する`; 15 passed, 2 failed | `total_count: 0` |
| [29132766507](https://github.com/sh4869221b/yosan-flow/actions/runs/29132766507) | `pull_request` / `3797d45c` | `00:30:44–00:34:34Z`; `09:30:44–09:34:34 JST` | `westus3`         | `dashboard-day-entry-delete-recalculation.spec.ts:21` timed out at 120 s filling `入力額 (円)`; 16 passed, 1 failed                                                                                   | `total_count: 0` |
| [29109907092](https://github.com/sh4869221b/yosan-flow/actions/runs/29109907092) | `push` / `30d5c49d`         | `17:08:17–17:11:55Z`; `02:08:17–02:11:55 JST` | `northcentralus`  | `dashboard-day-entry-delete-recalculation.spec.ts:22` timed out at 120 s clicking `保存する`; 16 passed, 1 failed                                                                                     | `total_count: 0` |
| [29053603880](https://github.com/sh4869221b/yosan-flow/actions/runs/29053603880) | `push` / `3078a906`         | `22:07:50–22:11:40Z`; `07:07:50–07:11:40 JST` | `westus`          | `dashboard-day-entry-delete-recalculation.spec.ts:22` timed out at 120 s clicking `保存する`; 16 passed, 1 failed                                                                                     | `total_count: 0` |

The four observed runs do not execute near JST midnight. This rejects describing the JST boundary candidate as confirmed for these failures; it does not by itself disprove that separate boundary bug.

## Current controller ordering (observed source fact)

`runClientEffect()` starts `Effect.runPromise()` without returning or awaiting it (`src/lib/dashboard/client-effect.ts`). In `submitDayEntryEffect()` (`src/lib/dashboard/day-entry-controller-state.svelte.ts`), the successful POST path currently:

1. sets the returned period summary;
2. updates `selectedRow` when `selectedDate` is non-null;
3. waits for `historyController.loadHistoryEffect(selectedDate)`;
4. calls `closeDayEntry()`;
5. clears `modalSaving`.

Therefore a save button click can complete at the browser-action level before history refresh and modal close finish. The failing specs immediately reopen or repeat entry operations without explicitly pairing the click with the successful POST response and modal-hidden state. This is supporting evidence for a stale modal/history completion race, not yet root-cause proof.

## Hypothesis ledger

| Candidate from Issue #224                                  | Status after baseline                                                                                                                                                                  | Evidence needed to decide                                                                                                                              |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| JST date-boundary race                                     | Inconclusive; **not confirmed**. All four named failures ran at 02:08, 07:07, or 09:30–09:41 JST and failed in day-entry modal operations, not fixed negative-adjustment assertions.   | Fixed-clock boundary RED at `23:59:59`/`00:00:01`, then shared-clock GREEN; keep separate from the observed day-entry race.                            |
| D1 state leakage between tests                             | Inconclusive. Reset behavior/counts were not logged and no artifacts survived.                                                                                                         | Positive-control stale row, reset, then repeated zero-count verification with request IDs/timestamps.                                                  |
| Previous test's asynchronous request completes after reset | Supported as a candidate by repeated first-spec/day-entry timing failures and unawaited client execution; not confirmed.                                                               | Deterministically delay history completion across close/reopen and capture stale-session RED; wait for exact mutation response/modal hidden for GREEN. |
| Fixed 500 ms warm-up is insufficient                       | Inconclusive. Failures occur after suite startup, but no ready-state timing exists.                                                                                                    | Replace or instrument the sleep with a named readiness observable and compare failure rate without weakening timeouts.                                 |
| Shared Wrangler/workerd process retains state              | Inconclusive. All failures used the shared server, with no cache/binding/state artifact.                                                                                               | Compare shared versus isolated server runs while logging service cache/reset state.                                                                    |
| Playwright UI wait condition is insufficient               | Supported as a candidate: failures occur at memo visibility, input fill, or save click; successful mutations are not explicitly paired with POST 2xx plus modal hidden. Not confirmed. | Add response/modal observability, first demonstrate deterministic stale-close RED, then require exact POST 2xx and hidden modal for GREEN.             |
| Hosted runner load/timeouts                                | Inconclusive. Failures span `westus`, `westus3`, and `northcentralus`; each failing suite reports about 2.7 minutes, but CPU/load metrics are absent.                                  | Record action/actionability timestamps and request completion; do not accept timeout increases alone.                                                  |
| Wrangler/Playwright/SvelteKit dependency regression        | Inconclusive. Named runs use different SHAs and one Wrangler Renovate branch, but current logs do not establish a dependency-version correlation.                                      | Compare same-SHA reruns and lockfiles/tool versions against the deterministic reproduction.                                                            |

## Planned RED/GREEN gates

1. **Day-entry generation race RED:** instrument or route-delay the successful day-add/history sequence; perform save, reopen a newer modal session, release the old history completion, and require the current implementation to demonstrate that old completion can close or overwrite the newer session. If 20 barrier-verified attempts cannot reproduce it, reject the production controller guard and leave controller source unchanged.
2. **Day-entry generation race GREEN:** if RED is proven, bind completion to the originating modal generation so an old completion cannot mutate/close a newer session. The same deterministic scenario must pass, with public API and timeout/retry policy unchanged.
3. **Mutation wait GREEN:** for every successful day-entry add, pair the click with the exact period/day POST 2xx response and then require `day-entry-modal` hidden before the next action. Negative validation/503 cases keep direct error assertions.
4. **JST boundary RED/GREEN:** independently reproduce test/server clock divergence at the declared boundary. Do not implement a shared clock unless the RED exists; require both boundary cases to pass after any clock fix.
5. **Reset/stale-state gate:** prove the detector sees deliberately seeded state before reset and zero state after reset for the declared repetition count.
6. **Final stress gate:** four successful-add specs at 400/400, negative-adjustment grep at 100/100, full repository CI-parity commands, real desktop/mobile browser flows, and a remote run with retained failure diagnostics.

## Impacted files and verification commands

Expected investigation surface (no behavior change is made by this record):

- `.github/workflows/ci.yml`, `.github/workflows/e2e.yml`, `playwright.config.ts`
- `src/lib/dashboard/client-effect.ts`, `src/lib/dashboard/day-entry-controller-state.svelte.ts`
- `tests/e2e/dashboard-day-entry-helpers.ts`, `tests/e2e/helpers/db.ts`
- `tests/e2e/dashboard-day-entry-add-edit.spec.ts`, `tests/e2e/dashboard-day-entry-delete-recalculation.spec.ts`, `tests/e2e/dashboard.spec.ts`
- `src/routes/api/__test/reset/+server.ts`, server clock/summary/cache modules named in Issue #224

Exact baseline/manual QA commands:

```bash
gh run view 29133029655 --log-failed
gh api repos/sh4869221b/yosan-flow/actions/runs/29133029655/artifacts
pnpm exec prettier --check docs/superpowers/plans/2026-07-12-e2e-flake-investigation.md
git diff --check
git status --short
```

Binary baseline PASS is the presence of both named day-entry failures in the first command and `total_count: 0` in the second. Main risks are drawing causality from log timing without a deterministic RED, weakening waits/timeouts, masking failure with retry, or losing diagnostics because failed-run artifact upload is absent.

## Final results (append-only)

Do not rewrite prior entries. Append dated results with the exact command, outcome, evidence path, hypothesis status change, cleanup receipt, and remaining risk.

- 2026-07-12: Baseline created. Four named failed runs and zero-artifact API results recorded. No hypothesis promoted to confirmed. Evidence: `.omo/evidence/issue-224/task-1-baseline.md`.
- 2026-07-12: Alternative hypotheses were bounded and falsified without speculative production edits. Evidence: `.omo/evidence/issue-224/task-7-hypotheses.md` and `.omo/evidence/issue-224/task-7-boundary.txt`.

| Hypothesis                                | Exact probe                                                                                                                                                             | Observed result                                                                                                                                                                                                                                      | Decision                                                                                                                                            | Production action                                                                                                                                                            | Evidence                                                                                                                      |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| JST boundary / fixed clock                | Unit/JST/fixed-`jstToday` command plus Playwright negative-adjustment grep at `--repeat-each=100 --workers=1`                                                           | Unit: 83/83. Browser: desktop 100/100 and mobile 100/100. Named Actions failures were not near JST midnight.                                                                                                                                         | Rejected for Issue #224                                                                                                                             | None; no clock seam or fixed clock.                                                                                                                                          | `task-7-boundary.txt`                                                                                                         |
| Reset residue                             | Ten cycles of reset → same period ID seed → empty history/summary check → add positive control → reset → absence check                                                  | 10/10 same-ID creates succeeded; fresh histories were empty; detector saw each seeded input before reset and none afterward; summaries returned 404 after reset.                                                                                     | Rejected at 10 repetitions                                                                                                                          | None; no reset transaction or restart.                                                                                                                                       | `task-7-hypotheses.md`                                                                                                        |
| Late asynchronous writes                  | Await add 200, reset 200, then state-based bounded polling of summary/history without fixed sleep                                                                       | Six polling samples kept summary absent and history free of the synchronized mutation.                                                                                                                                                               | Rejected for synchronized mutation completion                                                                                                       | None. Client-side unawaited lifecycle was separately decided by deterministic Todos 3-6.                                                                                     | `task-7-hypotheses.md`                                                                                                        |
| Fixed 500 ms warmup insufficient          | Ten real browser navigations with explicit `create-period-panel` visibility and measured readiness                                                                      | 10/10 ready; 102-115 ms, maximum 115 ms. No readiness failure or first-test correlation.                                                                                                                                                             | Rejected in this environment                                                                                                                        | None; do not replace the sleep under this todo's gate.                                                                                                                       | `task-7-hypotheses.md`                                                                                                        |
| Shared Wrangler/cache retention           | Shared server invocation: both focused adjustment specs 100 times each; separate server invocations: each spec 10 times                                                 | Shared 200/200; separate desktop 10/10 and mobile 10/10.                                                                                                                                                                                             | Rejected at bounded sample                                                                                                                          | None; no server restart/cache change.                                                                                                                                        | `task-7-hypotheses.md`                                                                                                        |
| Playwright UI wait condition insufficient | Todo 3 delayed-history deterministic RED; Todo 4 exact POST 2xx plus modal-hidden helper; Todo 5 generation-guard RED→GREEN; Todo 6 migrated success flows at repeat 20 | Todo 3 reproduced the stale close after exact POST success. Todo 4 proved exact URL/method response matching and modal-hidden lifecycle. Todo 5 made stale success/failure races green. Todo 6 passed success flows 80/80 and negative controls 4/4. | **Supported and confirmed as an Issue #224 contributor**; the baseline row was only “not confirmed” before the declared RED/GREEN evidence existed. | Keep the exact POST + modal-hidden helper, migrated successful-save tests, and modal-generation guard. Residual risk: remote CI stress/artifact proof remains a Todo 8 gate. | `task-3-red.txt`; `task-4-helper.txt`; `task-5-generation-guard.txt`; `task-6-success-flows.txt`; `task-6-negative-flows.txt` |
| Hosted runner load/timeouts               | Live `gh` region/image logs for four failures and passing run 29146170391                                                                                               | Failures span westus3, northcentralus, and westus; the pass shares westus with a failure. All use ubuntu-24.04 image `20260705.232.1`. Per-job CPU, memory, and I/O metrics are unavailable.                                                         | Region/image rejected; hosted load remains inconclusive because distinguishing telemetry is missing.                                                | None; no timeout increase. Residual risk is unmeasured hosted resource pressure.                                                                                             | `task-7-hypotheses.md`                                                                                                        |
| Dependency regression                     | Package/lock comparison for the same failed and passing runs                                                                                                            | All compared runs use pnpm 11.10.0 and Playwright ^1.61.1; failed 7f16491 and passing 693e492 both use Wrangler ^4.107.1.                                                                                                                            | Rejected as a distinguishing cause in the compared runs.                                                                                            | None; no dependency pin, rollback, or bump.                                                                                                                                  | `task-7-hypotheses.md`                                                                                                        |

The first negative-adjustment attempt overlapped Todo 6 and failed before test execution because two builds mutated shared `.svelte-kit` output. It is classified only as a resource-coordination collision, excluded from hypothesis outcomes, and was rerun successfully after terminating its managed Playwright/Wrangler/workerd tree and confirming port 4173 was free. The Playwright UI wait candidate is the one supported Issue candidate, and its required failing-first work was already completed by Todos 3-6; no additional Todo 7 production follow-up is justified. Residual risk is limited to hosted-runner load, for which the available Actions metadata has no CPU, memory, or I/O telemetry.

Candidate coverage audit: the eight Issue #224 candidates map one-to-one to the eight final rows above—JST boundary, D1 reset residue, previous asynchronous write, fixed warmup, shared Wrangler state, Playwright UI wait, hosted runner load/timeouts, and dependency regression. The baseline ledger intentionally retained pre-probe statuses; this final table supersedes those statuses with runtime decisions while preserving the investigation chronology.

- 2026-07-12: Todo 8 local terminal proof passed. The four successful-add specs passed 400/400; the negative-adjustment grep passed desktop 100/100 plus mobile 100/100 (200 total); `format:check`, `lint`, `check`, `test:unit` (83/83), `test:integration` (34/34), `build`, and `test:e2e` (22/22) all exited 0. Real Wrangler/Chromium QA confirmed desktop totals/history at 3000 after two saves, 3500 after editing 1000 to 1500, 1500 after deleting 2000, and the same 1500/single history row after reload/reopen; mobile 390x844 confirmed add 1200 and visible edit/delete/save/cancel controls. Console, page-error, request-failure, and HTTP error audits were empty. Evidence: `.omo/evidence/issue-224/task-8-stress.txt`, `task-8-negative-adjustment.txt`, `task-8-full.txt`, and `task-8-browser/`.
- 2026-07-12: Todo 8 remote artifact proof remains **pending**. The task branch has no remote ref and `gh run list --branch codex/issue-224-e2e-ci-flake` returned no runs. Existing base/failed runs have zero artifacts and predate the unpushed workflow upload changes. No push or workflow dispatch was authorized. Evidence: `.omo/evidence/issue-224/task-8-remote-artifact.md`. This keeps the plan incomplete even though all local gates passed.
- 2026-07-12: Todo 8 local and remote proof is **complete**. This entry explicitly supersedes the preceding pending state and run [29187611717](https://github.com/sh4869221b/yosan-flow/actions/runs/29187611717), whose successful artifact lacked `test-results/results.json` and motivated adding the JSON reporter. The independently verified replacement run [29188055867](https://github.com/sh4869221b/yosan-flow/actions/runs/29188055867), attempt 1, executed branch source SHA `fd71469afe62b8e4face66ecbad677c50cf5449c`: `Run E2E tests` independently succeeded with 22/22 passed, `Upload E2E diagnostics` independently succeeded, and the completed run concluded `success`. Artifact `e2e-diagnostics-29188055867-1` (ID `8258641048`) was nonexpired with configured seven-day retention; its downloaded inventory contained `test-results/results.json`, `playwright-report/index.html`, and two Wrangler logs. Parsing `results.json` proved 8 suites, 22 specs, 22 passed results, and stats `expected=22`, `skipped=0`, `unexpected=0`, `flaky=0`. This remote run proves the source at `fd71469afe62b8e4face66ecbad677c50cf5449c`; it does not claim that this later documentation-only append was exercised by that run. Evidence: `.omo/evidence/issue-224/task-8-remote-artifact.md` and `.omo/evidence/issue-224/task-8-cleanup.txt`.
- 2026-07-12: The F2 code-quality finding and post-review remediation are **complete**, and run [29189123189](https://github.com/sh4869221b/yosan-flow/actions/runs/29189123189) supersedes run `29188055867` as the final production/test SHA proof while preserving the earlier run as reporter-fix history. F2 found that reversed successful POST completion could publish an older period summary after a newer one; the RED unit run failed the reversed-success case with publications `[secondSummary, firstSummary]` (1 failed, 5 passed). The fix assigns monotonic submission sequences and advances the publication watermark only when a successful summary is applied, so a delayed older success cannot roll back a newer success while an older success still publishes if the newer submission fails. A typed lower-level `clickSaveAndWaitForDayEntryAddResponse` helper now owns the exact same-origin POST response waiter and click, removing raw successful race clicks; the existing successful-save helper composes it with 2xx and modal-hidden checks. GREEN evidence passed the generation unit matrix 6/6, the full unit suite 86/86, integration 34/34, `check`, `lint`, `build`, and focused real-Wrangler Chromium helper/race/failure coverage 9/9; the race spec also passed 10/10 at repeat 5. The final remote run, attempt 1, executed SHA `c56bbc4677b593bcbe028d409faceac9aa2a66da`: `Run E2E tests` independently succeeded with 22/22 passed, `Upload E2E diagnostics` independently succeeded, and the run concluded `success`. Artifact `e2e-diagnostics-29189123189-1` (ID `8258959266`) was nonexpired with seven-day retention; it contained `test-results/results.json`, `playwright-report/index.html`, and two Wrangler logs. Parsed JSON proved 8 suites, 22 specs, 22 test objects, 22 passed results, stats `expected=22`, `skipped=0`, `unexpected=0`, `flaky=0`, and zero top-level errors. This run proves production/test SHA `c56bbc4677b593bcbe028d409faceac9aa2a66da`; it does not claim this later documentation-only append was exercised by that run. Evidence: `.omo/evidence/issue-224/f2-remediation.md`, `.omo/evidence/issue-224/f2-code-quality.md`, and `.omo/evidence/issue-224/task-8-remote-artifact.md`.
- 2026-07-12: The final hydration-race debugging remediation is **complete**, and run [29189864080](https://github.com/sh4869221b/yosan-flow/actions/runs/29189864080) supersedes run `29189123189` as the final source/test SHA proof while retaining the earlier F2 chronology. The final-debug race command timed out at the initial history GET with 8 passed, 1 failed, and 1 not run. Four distinguishing hypotheses were investigated: exact GET non-2xx was rejected by 20/20 direct and browser 200 responses plus a permanent 503 control; lost listener/request ordering was rejected by 20/20 exact request/response observations with the listener armed before click; stale server/DB state was rejected by 20/20 reset/seed/direct-history controls and an isolated unchanged race run at 40/40; clicking an SSR-visible calendar button before Svelte attached its delegated handler was confirmed by a deterministic hydration toggle, where the pre-hydration raw click opened no modal and sent zero history requests. The new `openDayEntryAndWaitForHistory` contract waits for the delegated handler, arms the exact same-origin GET listener before clicking, requires the correct modal/date, checks response status outside the predicate, and waits for history loading to finish plus the seeded empty state. The hydration-toggle RED became GREEN after hydration, the fixed race scenarios passed 40/40, helper/failure controls passed 8/8 including initial-history 503, and `pnpm check` reported zero errors and warnings without sleep, retry, or timeout extension. The final remote run, attempt 1, executed SHA `fa169e9a32b0a83243a1cd95b9eafdd779e53826`: `Run E2E tests` independently succeeded with 23/23 passed, `Upload E2E diagnostics` independently succeeded, and the job/run concluded `success`. Artifact `e2e-diagnostics-29189864080-1` (ID `8259179410`) was nonexpired with seven-day retention; it contained `test-results/results.json`, `playwright-report/index.html`, and two Wrangler logs. Parsed JSON proved 8 suites, 23 specs, 23 test objects, 23 passed results, stats `expected=23`, `skipped=0`, `unexpected=0`, `flaky=0`, and zero top-level errors. This run proves source/test SHA `fa169e9a32b0a83243a1cd95b9eafdd779e53826`; it does not claim this later documentation-only append was exercised by that run. Evidence: `.omo/evidence/issue-224/final-debugging-audit.md`, `.omo/evidence/issue-224/final-debug-flake-remediation.md`, and `.omo/evidence/issue-224/task-8-remote-artifact.md`.
- 2026-07-13: The first ready-for-review follow-up at `07e7d763ae42e504b46135e152ec7399935cda97` resolved two summary/modal races found against `7f0f13332c`: per-period active-save counts plus refresh generations reconcile the authoritative period summary after concurrent saves, current period/response guards prevent cross-period rollback, and history request generations prevent older GET responses from replacing a newer modal's history. RED tests reproduced reversed server freshness, same-day reopen, overlapping authoritative refreshes, cross-period completion, and reversed history responses; GREEN passed unit 91/91 and real-Wrangler Playwright 24/24. CI run [29199359759](https://github.com/sh4869221b/yosan-flow/actions/runs/29199359759) passed format/lint, type check, unit, integration, build, E2E, diagnostics upload, and the aggregate quality gate.
- 2026-07-13: The second review follow-up addresses four findings against `07e7d76`: the latest history request now clears loading even when period/date ownership changes while still rejecting its stale payload; a committed POST closes its originating modal even if the reconciliation GET fails, preventing accidental duplicate submission; both diagnostic upload steps explicitly set `include-hidden-files: true`; and the new save-helper specs use the hydration-aware modal-open helper for their initial click. The first two RED unit cases failed with `historyLoading === true` and `modalOpen === true`, then passed after the fix. Final local gates passed format, lint, type check, unit 93/93, integration 34/34, build, real-Wrangler Playwright 24/24, focused helper/race Playwright 7/7, and `actionlint`. Artifact note: the earlier artifact `8259179410` already contained two Wrangler logs despite the action's documented hidden-file default; the explicit flag therefore locks the intended collection contract rather than claiming every earlier artifact omitted those files. Remote CI proof for this follow-up is pending its commit and push.
- 2026-07-13: The third review follow-up scopes the applied-success submission watermark by period. A deterministic RED submitted period A first, applied a later period B save, returned to A, then completed A while both reconciliation GETs failed; the global watermark incorrectly left A at `usedYen: 0` instead of the committed `2000`. The per-period watermark made the same test pass twice without changing same-period stale-response protection. Local gates passed format, lint, type check, unit 94/94, integration 34/34, build, real-Wrangler Playwright 24/24, and the TypeScript no-excuse audit. Remote CI proof is pending its commit and push.
- 2026-07-13: Documentation-completeness follow-up for the preceding entry. Exact RED command `pnpm exec vitest run tests/unit/day-entry-controller-period-sequence.test.ts` failed 1/1 at `16d4032` with period A `usedYen: 0` instead of `2000`; after `4045153`, the same command passed 1/1 twice. Exact local gate sequence was `pnpm format:check && pnpm lint && pnpm check && pnpm test:unit && pnpm test:integration && pnpm build`, followed by `pnpm test:e2e`; outcomes were unit 94/94, integration 34/34, Playwright 24/24, zero Svelte errors/warnings, and successful format, lint, and build. Evidence path: `tests/unit/day-entry-controller-period-sequence.test.ts`; originating review: [PR #230 comment](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570344943). Hypothesis status changed from open to **confirmed** for the cross-period global-watermark mechanism; selection mismatch and reconciliation overwrite were refuted by returning to A before resolving its POST and forcing the A GET to fail. Cleanup receipt: `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed, the debug-marker scan was empty, and `git status --short` was clean after `4045153`. Remaining risk: remote CI and its retained diagnostics do not cover `4045153` until this branch is pushed.
- 2026-07-13: The fourth review follow-up addresses three findings against `a50c84b`: originating modal completion now occurs before awaited history/summary reconciliation, matching successful responses advance their period watermark even while another period is selected, and summary/modal/history synchronization shares the same latest-success decision. Exact RED command `pnpm exec vitest run tests/unit/day-entry-controller-refresh-failure.test.ts tests/unit/day-entry-controller-latest-success.test.ts` produced 3 failed and 1 passed: the modal stayed open during a deferred reconciliation GET, an older period-A response rolled `usedYen` from `5000` back to `2000` after an off-screen newer success, and a reopened modal row independently rolled back from `5000` to `2000`. Exact GREEN command `pnpm exec vitest run tests/unit/day-entry-controller-refresh-failure.test.ts tests/unit/day-entry-controller-latest-success.test.ts tests/unit/day-entry-controller-period-sequence.test.ts tests/unit/day-entry-controller-review-races.test.ts tests/unit/day-entry-controller-generation.test.ts` passed 15/15. `pnpm format:check && pnpm lint && pnpm check && pnpm test:unit && pnpm test:integration && pnpm build` passed with unit 97/97, integration 34/34, and zero Svelte errors/warnings; `pnpm test:e2e` passed real-Wrangler Chromium 24/24, and the TypeScript no-excuse audit reported no violations. Evidence paths: `tests/unit/day-entry-controller-refresh-failure.test.ts`, `tests/unit/day-entry-controller-latest-success.test.ts`, and `tests/unit/day-entry-controller-generation.test.ts`; originating reviews: [non-blocking reconciliation](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570509798), [off-screen success ordering](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570509800), and [stale modal row rejection](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570509805). All three hypotheses changed from open to **confirmed** by their independent RED→GREEN toggles. Cleanup receipt: `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed, the debug-marker scan was empty, `git diff --check` passed, and `git status --short` contained only the intended controller, test, and append-only plan changes. Remaining risk: remote CI and retained diagnostics do not cover this fourth follow-up until its commit is pushed.
- 2026-07-13: Independent review of `aa5df82` found two coverage/integrity gaps before push. The modal-completion contract covered a pending summary GET but not a pending history GET, and tying history refresh to the latest POST body could leave history stale when an older client submission committed last. The fix keeps stale POST bodies rejected for summary/row state while allowing their authoritative history GET, adds a deferred-history completion test, and strengthens the reverse-order test to require a second history refresh after the late commit. The focused command covering five controller files passed 16/16; the full sequence `pnpm format:check && pnpm lint && pnpm check && pnpm test:unit && pnpm test:integration && pnpm build && pnpm test:e2e` passed with unit 98/98, integration 34/34, Playwright 24/24, and zero Svelte diagnostics. `test-results/` and `playwright-report/` were removed and `git diff --check` passed. Remaining risk: remote CI and retained diagnostics remain pending until the final review commits are pushed.
- 2026-07-13: Reproducibility addendum for the independent-review follow-up. Exact focused command `pnpm exec vitest run tests/unit/day-entry-controller-refresh-failure.test.ts tests/unit/day-entry-controller-latest-success.test.ts tests/unit/day-entry-controller-period-sequence.test.ts tests/unit/day-entry-controller-review-races.test.ts tests/unit/day-entry-controller-generation.test.ts` passed 16/16. Evidence paths are `tests/unit/day-entry-controller-refresh-failure.test.ts` for the pending-history completion boundary and `tests/unit/day-entry-controller-generation.test.ts` for the second authoritative history refresh after a late stale-body response. Hypothesis status changed from open to **confirmed** for both review findings: a deferred history effect proved completion was already non-blocking, while the reverse-order call-count assertion failed before history refresh was separated from stale-body publication and passed afterward. Remaining risk is unchanged: final remote CI is pending push.
- 2026-07-13: The fifth review follow-up addresses [summary reconciliation blocked by history](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570688451). A deterministic pending-history RED, `pnpm exec vitest run tests/unit/day-entry-controller-refresh-failure.test.ts`, failed 1/4 because only the POST fetch occurred instead of the expected POST plus authoritative period-summary GET. Moving the existing `remainingSubmissions === 0` summary reconciliation block before the awaited history Effect made the same file pass 4/4 twice while retaining history session cancellation. The five-file controller command passed 17/17; `pnpm format:check`, `pnpm lint`, `pnpm check`, `pnpm test:unit`, `pnpm test:integration`, and `pnpm build` passed with unit 99/99, integration 34/34, and zero Svelte diagnostics; the TypeScript no-excuse audit reported no violations in the two changed TS files; real-Wrangler Chromium `pnpm test:e2e` passed 24/24. Evidence path: `tests/unit/day-entry-controller-refresh-failure.test.ts`. Hypothesis status changed from open to **confirmed** for sequential history blocking, while refresh ownership/count suppression and modal-close cancellation were refuted. Cleanup receipt: the temporary no-excuse dependency symlink was removed immediately after the audit; `.debug-journal.md`, `test-results/`, and `playwright-report/` remain scheduled for final cleanup before commit. Remaining risk: remote CI does not cover this follow-up until push.
- 2026-07-13: Final cleanup addendum for the fifth follow-up. `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed; the temporary no-excuse dependency symlink was absent; the debug-marker scan was empty; `git diff --check` and the final `pnpm format:check` passed; and the worktree contained only the intended controller, unit-test, and append-only plan changes. Remote CI remains the only pending gate until push.
- 2026-07-13: The sixth review follow-up addresses [old-day history starting after summary reconciliation](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570818415). A deterministic RED held the authoritative period-summary GET, opened another day, then released the GET; `pnpm exec vitest run tests/unit/day-entry-controller-review-races.test.ts` failed 1/5 because `loadHistoryEffect("2026-07-12")` was called once. The controller now rechecks current period plus modal-generation/date ownership immediately before constructing the history Effect, preventing an old request from incrementing the shared history generation while retaining same-session and same-day reopen refreshes. The five-file controller command passed 18/18. Local gates passed format, lint, type check, unit 100/100, integration 34/34, build, and the TypeScript no-excuse audit for all three changed TypeScript files. A real-Wrangler Chromium test reproduced the exact delayed-summary/day-switch flow and observed zero old-day history requests; the full Playwright suite passed 25/25. Hypothesis status changed to **confirmed** for the captured pre-await history flag, while Deferred cancellation and harmless history-generation guards were refuted. Cleanup receipt remains pending the final artifact sweep before commit. Remaining risk: remote CI and retained diagnostics do not cover this follow-up until push.
- 2026-07-13: Final cleanup addendum for the sixth follow-up. `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed; the temporary no-excuse dependency symlink was absent; the debug-marker scan was empty; `git diff --check` passed; and the worktree contained only the intended controller, unit/E2E regression tests, and append-only plan changes. Remote CI remains the only pending gate until push.
- 2026-07-13: The seventh review follow-up addresses [loss of the fullest successful summary](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3570947234). A deterministic RED submitted two days concurrently, resolved the second response with only its committed `3000`, then resolved the first response with the complete `2000 + 3000` summary while the final period-summary GET returned 503; `pnpm exec vitest run tests/unit/day-entry-controller-summary-completeness.test.ts` failed 1/1 because submission ordering retained the partial `plannedTotalYen: 3000`. Successful add responses are now compared by `plannedTotalYen` within the active same-period concurrent-add batch, so the most complete observable summary is retained until authoritative reconciliation completes or fails; the batch watermark is deleted when its active submission count reaches zero so later overwrite/history edit/delete decreases remain valid. The focused controller command passed 19/19, and the exact real-Wrangler Chromium scenario passed 1/1 with both calendar values preserved after the forced 503. Local gates passed format, lint, type check, unit 101/101, integration 34/34, build, full Playwright 26/26, and the TypeScript no-excuse audit for all three changed TypeScript files. The add-only monotonicity hypothesis is **confirmed** for nonnegative same-period adds, submission order and response-arrival order are **refuted** as freshness signals, and a persistent cross-operation maximum is **refuted** because overwrite/history mutation may reduce totals. Cleanup receipt and remote CI proof remain pending until final commit and push.
- 2026-07-13: Final cleanup addendum for the seventh follow-up. `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed; the temporary no-excuse dependency symlink was absent; the debug-marker scan was empty; `git diff --check` passed; and the worktree contained only the intended controller, new unit/E2E regression tests, and append-only plan changes. Remote CI remains the only pending gate until push.
- 2026-07-13: The eighth review follow-up addresses [stale add reconciliation after history mutation](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3571224477), [stale history loads after row mutation](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3571224481), and [discarded off-screen successful save bodies](https://github.com/sh4869221b/yosan-flow/pull/230#discussion_r3571224487). Independent RED cases proved all three: a held period-summary response restored edited `usedYen` from `1000` to `2000`; a held history response replaced the edited `1000` row with its old `2000` body; and returning to period A before its remaining submission failed plus a final GET 503 left the committed off-screen `2000` body unavailable and the summary at `0`. The history controller now advances a period-scoped mutation sequence and invalidates outstanding history loads whenever PATCH/DELETE settles; add response candidates and reconciliation GETs are accepted only within the same mutation sequence. The active add tracker retains the fullest complete summary body, publishes it when its period becomes current on a later completion, and clears it when the batch settles, preserving later legitimate decreases. The controller was kept at 248 pure LOC by extracting the 59-line tracker and shared nine-line summary-row lookup. Focused GREEN passed 24/24 across eight controller files. Local gates passed format, lint, type check, unit 104/104, integration 34/34, build, full Playwright 27/27, and the TypeScript no-excuse audit for all 12 changed TypeScript files. Real Wrangler/D1/Chromium captured a stale `2000` summary, edited to `1000`, captured a stale history response, edited again to `500`, then released both stale bodies; calendar and history remained `500` (1/1). Cleanup receipt and remote CI proof remain pending until final commit and push.
- 2026-07-13: Final cleanup addendum for the eighth follow-up. `.debug-journal.md`, `test-results/`, and `playwright-report/` were removed; the temporary no-excuse dependency symlink was absent; the debug-marker scan was empty; `git diff --check` passed; and the worktree contained only the intended dashboard controllers/helpers, focused unit/E2E regressions, mechanical test-double contract updates, and append-only plan changes. Remote CI remains the only pending gate until push.
- 2026-07-13: Independent review of the eighth follow-up found two additional ordering defects before push. First, a period-A history mutation settling after period B started a history GET advanced the global history generation, discarded B's response, and published A's mutation body into B. The deterministic RED expected B's `500` history but retained A's `1000` body. History-load invalidation now checks the active request's owning period, and mutation publication is guarded by the originating period, date, mutation generation, and response `periodId`. Second, an off-screen retained `plannedTotalYen: 2000` candidate could replace a newer current `5000` summary when another submission settled and final reconciliation returned 503. The deterministic RED rolled `5000` back to `2000`; retained candidates now publish only when they are more complete than the current same-period summary. A direct regression also proves that a POST body captured before a history mutation is rejected by the mutation-sequence guard. Focused unit verification passed 7/7, and real Wrangler/D1/Chromium passed both the same-period stale-response race and the new delayed-period-A-mutation/period-B-history-load flow 2/2. Final local gates passed format, lint, type check, unit 107/107, integration 34/34, build, full Playwright 28/28, `git diff --check`, and the TypeScript no-excuse audit; the day-entry controller remains below the 250 pure-LOC ceiling at 249. Cleanup receipt and final remote CI proof remain pending until commit and push.
- 2026-07-13: A third independent review pass closed two remaining completeness gaps. History-load ownership now includes both period and date, so a mutation for day A invalidates only day A's outstanding GET and does not discard a newer day B history load in the same period. Mutation publication is split by scope: the successful period summary still updates the current same-period dashboard after a date switch, while histories and errors remain owned by the originating date. A focused regression exercises delayed day-A mutation, active day-B history GET, period-summary application, and final day-B history publication. The confirmed hydration race mitigation was also applied to the initial modal open in the four original successful-flow specs (`add-edit`, `delete-recalculation`, `mobile-controls`, and `modal-state-reset`) using the hydration-aware exact-history-response helper instead of an SSR-visible raw click. Focused unit verification passed 7/7; the four migrated specs plus both mutation-race browser scenarios passed 6/6; and final local gates passed format, lint, type check, unit 108/108, integration 34/34, build, and full Playwright 28/28. Cleanup receipt and final remote CI proof remain pending until commit and push.
- 2026-07-13: A fourth independent code-quality pass found a cross-kind rollback: a history PATCH response could capture `500`, remain delayed while a later day add published the complete `2500` summary, then overwrite the newer summary when released. Dashboard composition now shares a period-scoped summary revision across period, day-entry, and history controllers. Every summary publication advances that revision; a history mutation whose starting revision is stale discards its response body and performs authoritative summary reconciliation, plus an originating-date history refresh when that date is still current. The deterministic unit regression passed within a 14/14 focused matrix, and a real Wrangler/D1/Chromium scenario held the PATCH response, completed the newer add, released the stale response, observed the reconciliation GET, and retained `500 + 2000` (1/1). The history controller's duplicate PATCH/DELETE lifecycle was consolidated to keep it at 243 pure LOC; the day-entry and period controllers remain within the structural ceiling at 250 and 249 pure LOC. Final gates passed format, lint, type check, unit 109/109, integration 34/34, build, and full Playwright 29/29. Cleanup receipt and final remote CI proof remain pending until commit and push.
