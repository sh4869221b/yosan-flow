# Copy this file to `.dev.vars` for local `wrangler dev` settings.
# Keep secrets out of source control.

# Optional: force in-memory fallback in local development/tests.
# 0 or unset: use D1 binding
# 1: force in-memory mode
YOSAN_FLOW_FORCE_IN_MEMORY_DEV=0
